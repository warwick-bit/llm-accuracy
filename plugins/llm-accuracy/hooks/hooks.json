{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; p=r'${CLAUDE_PLUGIN_ROOT}/hooks/analysis-contract-injector.py'; os.execvp('python3', ['python3', p]) if os.path.exists(p) else None\""
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; p=r'${CLAUDE_PLUGIN_ROOT}/hooks/fusion-evidence-trigger.py'; os.execvp('python3', ['python3', p]) if os.path.exists(p) else None\""
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; p=r'${CLAUDE_PLUGIN_ROOT}/hooks/post-compact-accuracy.py'; os.execvp('python3', ['python3', p]) if os.path.exists(p) else None\""
          }
        ]
      }
    ]
  }
}
